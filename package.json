{
  "name": "glitchbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "node -v && npm -v && npm clean-install && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  },
  "devDependencies": {
    "vite": "^6.3.6"
  },
  "engines": {
    "node": ">=18"
  }
}
